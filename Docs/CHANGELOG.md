# سجل إصدارات الدليل — Changelog / Changelog

> تاريخ التوثيق مقابل إصدارات التطبيق (`package.json`). القاعدة: كل إصدار تطبيق يغيّر سلوكاً ظاهراً للمستخدم يستلزم إدخالاً هنا وتحديث الختم في `ar/README.md` و`en/README.md`.

## v0.25.8 — إصلاح تشغيل سطح المكتب + تفعيل صفحة حول النظام (2026-09-23)

- **سطح المكتب:** `electron/main.js:155` كان يحوي `as { channel?: string }` (نحو TypeScript) داخل ملف ESM عادي → `SyntaxError: Unexpected identifier 'as'` عند التشغيل — أزيلت الصياغة. **الترجمات:** `settings.about` (عنوان/وصف/إصدار/روابط/تلميح القناة) + `sidebar.settings.about: "حول النظام"/"About"` في `ar/en` متوازنة — رابط `/settings/about` يعمل داخل `settings` (مجموعة `system`).

## v0.25.7 — JEV عبر العملية الرئيسية + تشخيص صادق (2026-09-23)

- `electron/main.js` يمرر استدعاءات JEV عبر العملية الرئيسية بدل الشبكة المباشرة + تشخيص أخطاء JEV الصادقة + `tsc` نظيف.

## v0.25.6 — كشف التحديث وطلب التنزيل (ويب/سطح مكتب/موبايل) (2026-09-23)

- **ويب/موبايل:** `useUpdateCheck` يفحص `GitHub Releases API` (قناة `stable`/`beta` خلف `settings.edit`) مع سقوط إلى `/version.json`، كل 6 ساعات + عند العودة + زر يدوي. `UpdateBanner` في `AppLayout` يعرض `يتوفر {{latest}} — أنت على {{current}}` مع `تحديث الآن` (`location.reload`) و`لاحقاً` و`ما الجديد`.
- **سطح المكتب:** `electron-updater@^6` + `publish: {github: AhmedAlmaghz/MaghzAccountFlash4}` + `generateUpdatesFilesForAllChannels:true` + `electron/main.js` (`autoDownload` في الخلفية، `update:available/downloaded/progress`) + `preload` جسر `window.electronUpdater` + `release.yml` يبني `nsis + portable` مع `latest.yml/blockmap`. **بلا شهادة حالياً** (`CSC_IDENTITY_AUTO_DISCOVERY=false` — تحذير SmartScreen حتى تتوفر الشهادة).
- **الإعدادات:** جديد `AboutPage` (`/settings/about`) يعرض `APP_VERSION_LABEL` وحالة التحديث و**قناة التحديث [مستقر/تجريبي]** خلف `settings.edit` فقط + زر `التحقق الآن`.
- **البناء:** `vite.config.ts` يولّد `dist/version.json` `{version, channel, builtAt}`؛ `tsc 0`/`build` أخضر.

## v0.25.4 — إصلاحات الاتصال الشامل + استقرار الوكيل (2026-09-23)

- **البذر المحلي**: `runPgliteMigrations` يعيد المحاولة بعد فشل عابر (قفل IndexedDB) عبر `resetPgliteMigrationsCache()` + بذر `seedDefault/demo` يعيد المحاولة تلقائياً عند `relation does not exist` — أغلق `companies does not exist` في الخطوة 4.
- **Neon على الويب**: `DatabaseStep.handleNext` يحفظ `DATABASE_URL` في الخزنة (`saveRemoteConnection` + `setStoredActiveRemoteId`) قبل الانتقال — `SeedStep` يدافع بخزنة احتياطية من `onboardingStore` — أغلق `No remote database selected` بعد اختبار ناجح.
- **جدار المتصفح**: `connectionVault.saveRemoteConnection` يرفض `webTcpUnsupported` مبكراً + `connection.ts` يوثق سياسة `PGlite محلي في كل مكان / Neon HTTP على الويب / أي مزود TCP على سطح المكتب` + واجهة الأونبوردينج تظهر تحذير `webTcpDesc` و`remoteWebNote` + منع حفظ Supabase على الويب برسالة صادقة.
- **Supabase ENOTFOUND**: `electron/dbHandler.js:db:test-connection` يترجم `ENOTFOUND/ETIMEDOUT/28P01/SSL` إلى إرشاد عربي (تحقق من المضيف/المنفذ 5432 مقابل 6543 للـ pooler/المشروع النشط/كلمة المرور/`?sslmode=require`).
- **مصادقة دفعات الوكيل**: `getSession(token)` يهاجر `webContentsId` بعد إعادة التحميل بدل رفض التوكن (كان كل تحديث يقتل الدفعات بـ `Authentication required`) + `errorTaxonomy` تصنف `Authentication/Login required` كـ `PERMISSION_DENIED` غير قابل لإعادة المحاولة (بدل `UNKNOWN` القابل) — الدفعات تفشل بصدق وتوجّه لإعادة تسجيل الدخول بدل الحلقة اللانهائية.
- **الاستقرار**: `eslint.config.js` يعطّل `no-useless-assignment` الضوضائي + `tsc 0`/`eslint 0`/`build` أخضر.

## v0.25.3 — مسح i18n الشامل + نقل تقارير JEV (2026-09-23)

- **مسح i18n الشامل**: ماسح آلي عبر 430 ملفاً كشف 272 مفتاحاً ناقصاً + 14 عائلة متعارضة string↔object (الأب يُحفظ كـ `{title}`) — أصلحت جميعاً مع إعادة توجيه ~60 موقع استخدام؛ التوازن الحالي **3959=3959** (متوازنة ar==en).
- **الطباعة والإيصالات تتبع لغة الواجهة**: `printDocument` و `POS` يمرران `t` من 12 موقع استدعاء إلى قوالب `print.*`/`pos.receipt.*` مع بدائل عربية عند غياب `t`.
- **نقل تقارير JEV**: `Docs2/` ← `docs_dev/JEV Docs/` (JEV-Execution-Summary + JEV-Implementation-Plan + JEV-Integration-Report) — المراجع في `AGENTS.md` و`README.md` و`Docs/README.md` و`Docs/*/CONVENTIONS.md` و`docs_dev/README.md` و`JEV-Execution-Summary.md` حُدّثت، و`AGENTS.md` رُفع إلى v0.25.3 (38 migration و3959 مفتاح).

## v0.23.0 — قاعدة البيانات الشاملة + لغات الأونبوردينج (2026-09-22)

- **اتصال واحد لكل المنصات**: الصق `DATABASE_URL` واحداً (محلي، Supabase، Neon، Google Cloud، خاص) في الإعدادات ← قاعدة البيانات أو الأونبوردينج — تحليل تلقائي للمزود + اختبار دون حفظ + حفظ مشفر + تفعيل. سطح المكتب يتصل TCP المباشر بأي مزود؛ الويب/الجوال يعمل محلياً (PGlite) أو عبر Neon (HTTP) مع إرشاد صريح لما عداهما.
- **ثبات الاتصال**: إعدادات الخادم تُحفظ مشفرة بمفتاح النظام وتبقى عبر إعادة التشغيل (كانت ذاكرة-فقط)؛ SSL بتوافق `psql` حرفياً؛ نفس مخطط drizzle يُرحَّل على كل الخلفيات.
- **الأونبوردينج بلغتك**: يفتح بلغة الجهاز تلقائياً (عربي/English) مع مبدل ظاهر في الترحيب + حقل رابط اتصال يعبئ الحقول تلقائياً.
- **إصلاح المثبت**: التطبيق المعبأ كان يموت بصمت بلا `.env.local` قبل فتح أي نافذة — أصبح يعمل افتراضياً على القاعدة المحلية + سجل إقلاع تشخيصي + اسم قطعة منفصل لنسخة portable.

## v0.21.1 — الأمثلة التدريبية العشرة (2026-09-20)

- `ar/Examples/` + `en/Examples/`: 10 أمثلة محلولة (تجزئة، استيراد وعملات، ضريبة سعودية، تصنيع، خدمات ورواتب، صيدلية FIFO، كافيه POS، مقاولات وأصول، مدرسة، جملة وائتمان) — كل مثال: بطاقة شركة + افتتاحي + عمليات + قيود مرحّلة + ميزان + دخل + مركز مالي + تدفق نقدي + قائمة مقارنة. جميع الأرقام موثقة حسابياً آلياً (سكربت تحقق خارج المستودع: كل قيد متوازن ومعادلة الميزانية صفرية).
- فهرس `Examples/README.md` باللغتين + صف الفهرس الرئيسي + شجرة البنية في `Docs2/README.md`.

### توسيع الأمثلة — دورة نظام كاملة (2026-09-20)

- كل مثال أصبح دورة كاملة: بيانات أساسية (صناديق مربوطة بحسابات، عملاء وموردون، موظفون، منتجات ووحدات وباركود، مخازن، BOM أو عميل محتمل) ← عمليات **مؤرخة** بمسار الشاشة ← قيود ← قوائم ← قيد إقفال `CLS` مولّد آلياً مع ميزان ما بعد الإقفال.
- معاملات جديدة موثقة آلياً: سدادات مربوطة، مرتجعات مشتريات، أصول وإهلاك (01/08)، سلف وعهد وتسوياتها (03/05/07/09/10)، توريد استقطاعات (05/09)، وردية فائض (07)، تحويل صناديق (02)، تحصيلات إضافية.
- خريطة تغطية العناصر في فهرسي الأمثلة.

## v0.21.1 — الاستكمال: الأصول والسكربتات الإنجليزية وAGENTS (2026-09-20)

- فحص روابط الصور: الملفات الجديدة بلا مراجع صور عمداً (لا روابط مكسورة) + ملحق «لقطات مطلوبة» بجدول 8 لقطات (المسار واسم الملف) في سكربتات `ar` و`en`.
- `videos/en/scripts/15-19-batch-scripts.md`: المرآة الإنجليزية للسكربتات الخمسة + ملحق اللقطات.
- `AGENTS.md`: الترويسة `v0.2.0` ← `v0.21.1` (بالحقائق المثبتة: 38 migration، 3246 مفتاح i18n، 14 وحدة) + صف وحدة `tax/` (القسم أصبح 14 وحدة).

## v0.21.1 — الدفعة (ب): التقييم والفروق والولايات والتحديثات (2026-09-20)

### جديد (ar + en)
- `07-inventory/04-valuation.md`: طرق التقييم الثلاث (`moving_average`/`fifo`/`standard`) + طبقات `inventory_layers` + فروق الأسعار `51901` + مثال FIFO رقمي ومثال معياري.
- `16-multicurrency/02-fx-revaluation.md`: الفروق المحققة بسعر الفاتورة (قيد `-FX` على `52902` باتجاهه الصحيح) + إعادة التقييم الدورية بمرساة `last_reval_rate` + مثال رقمي.
- `17-tax-jurisdictions/README.md`: قسم جديد — ملف لكل دولة (`SA` 15% / `AE` 5% / `EG` 14% / `YE` 0%) + بطاقة الولاية `tax.country_code` + التحقق الآلي + مثال ضبط شركة سعودية.
- `videos/ar/scripts/15-19-batch-scripts.md`: سكربتات تصوير 15–19 (نصوص فقط، بلا إنتاج فيديو).

### محدّث (ar + en)
- `12-hr/02-payroll-eos.md`: قيد Gross-up الرقمي (507,500/27,000/480,500 على `52101`/`21501`/`21502`) + مخصص الإجازات `21504` (IAS 19).
- `10-pos/02-shifts.md`: ترحيل فرق الصندوق التلقائي (`POS-DIFF` على `52901`/`41901`) + العميل النقدي التلقائي `CASH`.
- `11-manufacturing/README.md`: ضوابط الإكمال الدقيقة (مستودع التوريد الافتراضي، رفض الإنتاج الصفري، الحارس الناعم 53xxx، الحسابات القابلة للتكوين).
- `15-ai-assistant/README.md`: المعاينة المسبقة (`ai.preview_batch` فوق 10 عمليات) + ذاكرة الجلسة (سجل المهمة وجدول المهام).
- `07-inventory/README.md` و`16-multicurrency/README.md`: روابط الملفات الجديدة.
- `ar/README.md` و`en/README.md`: صف القسم 17 + روابط التقييم والفروق.
- `Docs2/README.md`: «16 قسماً» ← «17 قسماً» + شجرة البنية.

## v0.21.1 — الدفعة (أ): الحوكمة والإقفال والضرائب (2026-09-20)

### جديد (ar + en)
- `06-accounting/05-financial-controls.md`: فصل الضوابط المالية وتقييد العمليات — خريطة الحوكمة الكاملة (الصلاحيات، الترحيل الذري، نهائية المُرحَّل، قفل الفترات والسنوات، سجل التدقيق، بوابات المخزون والنقدية والائتمان) مع مثال بيع نقدي كامل بضريبة وتكلفة.
- `06-accounting/06-year-end-assets.md`: الإقفال السنوي بقيد `CLS-YYYY` + سجل الأصول الثابتة (رسملة إجبارية، إهلاك ثابت/متناقص، استبعاد) + العكس الحقيقي (`REV-` للقيود، مرتجع الباقي للفواتير، مرآة الأرجل للسندات).
- `06-accounting/07-vat-periods.md`: فصل ضريبة المدخلات `21302` عن المخرجات `21301` + دورة الفترات الضريبية (open/closed/filed) + الإقرار المحسوب من أرجل القيود المرحّلة + جدول الدول (`SA` 15% / `AE` 5% / `EG` 14% / `YE` 0%).

### محدّث
- `ar/README.md` و`en/README.md`: الختم `v0.15.18` ← `v0.21.1` + صفوف الفهرس الثلاثة الجديدة.
- `ar/06-accounting/README.md` و`en/06-accounting/README.md`: خريطة القراءة من 4 خطوات إلى 7 + جدول الملفات.

## v0.15.18 — الأساس الموثق سابقاً

- 16 قسماً + 5 ملاحق (ar ↔ en مرآة كاملة)، القالب الموحد، جدول المصطلحات، البادئات، الأدوار الستة، ~80 لقطة لكل لغة، 14 فيديو عربي.

## سياسة الترقيم

- رقم الدليل يطابق `package.json` (`version`) — لا أرقام مستقلة للتوثيق.
- أي دفعة توثيق جديدة تضيف إدخالاً أعلى هذا الملف بالتاريخ والملفات المتأثرة.
